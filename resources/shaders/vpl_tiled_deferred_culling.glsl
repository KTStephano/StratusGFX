STRATUS_GLSL_VERSION

// These needs to match what is in the renderer backend!
// TODO: Find a better way to sync these values with renderer
#define MAX_TOTAL_VPLS_PER_FRAME (128)
#define MAX_VPLS_PER_TILE (16)